import styled from 'styled-components';

const StockDetailsSections = styled.section`
  background-color: var(--color-secondary-background);
  height: 100vh;
  padding-block-start: 2rem;
`;

export default StockDetailsSections;
