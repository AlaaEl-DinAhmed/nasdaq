import styled from 'styled-components';

const StockPrevCloseWrapper = styled.article`
  background-color: var(--color-background);
  padding-block: 1rem;
  padding-inline: var(--padding-start);
`;

export default StockPrevCloseWrapper;
