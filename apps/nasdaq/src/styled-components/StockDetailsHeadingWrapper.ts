import styled from 'styled-components';

const StockDetailsHeadingWrapper = styled.span`
  display: block;
  padding-block: 1rem;
  padding-inline: var(--padding-start);
`;
export default StockDetailsHeadingWrapper;
